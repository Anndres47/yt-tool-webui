version: '3'
services:
  ytwebui:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./data:/app/downloads
    environment:
      - YTDL_DOWNLOAD_PATH=/app/downloads
    restart: unless-stopped
