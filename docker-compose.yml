version: '3'
services:
  yt-tool-webui:
    build: .
    ports:
      - "8047:8047"
    volumes:
      - ./data:/app/downloads
    environment:
      - YTDL_DOWNLOAD_PATH=/app/downloads
    restart: unless-stopped
